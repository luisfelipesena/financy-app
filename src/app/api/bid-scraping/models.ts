export namespace Team {
	export interface Default {
		codigoClube: string;
		clube: string;
		uf: string;
		tipoClube: string;
		isformador: string;
	}

	export interface ApiResponse {
		codigo_clube: string;
		clube: string;
		uf: string;
		tipo_clube: string;
		isformador: string;
	}
}
